import { Response } from "express";
import { IAuthor } from "../interface/AuthorInterface";
import { IAuthLogin, IUser } from "../interface/UserInterface";

class AuthModel {
  auth({  email, password }: IAuthLogin){
      
  }
}

export default new AuthModel();
